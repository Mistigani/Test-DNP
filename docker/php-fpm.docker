FROM php:8.4-fpm

RUN apt-get update && apt-get install -y \
    firebird-dev \
    && docker-php-ext-install pdo_mysql pdo_firebird \
    && pecl install redis \
    && docker-php-ext-enable redis

WORKDIR /var/www